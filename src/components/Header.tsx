import styled from 'styled-components';

export default function Header() {
    return (
        <Container>
            <p>Header</p>
        </Container>
    );
}

const Container = styled.div`
    width: 100%;
`;